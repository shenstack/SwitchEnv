## Summary / 概述

<!-- 简要描述这个 PR 做了什么以及为什么。 -->

## Related Issue / 关联 Issue

<!-- 关联相关 Issue。使用 "Fixes #123" 可在合并时自动关闭。 -->

Fixes #

## Screenshots / 截图

<!-- 如有需要，请添加修改前后的截图。 -->

| Before / 修改前 | After / 修改后 |
|-----------------|---------------|
|                 |               |

## Checklist / 检查清单

- [ ] `npm run typecheck` passes / 通过 TypeScript 类型检查
- [ ] `npm run build` passes / 通过前端构建
- [ ] `cargo clippy -- -D warnings` passes（如修改了 Rust 代码）
- [ ] `cargo fmt --check` passes（如修改了 Rust 代码）
- [ ] `cargo test` passes（如修改了 Rust 代码）
